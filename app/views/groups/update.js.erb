// update name, pic and description on group page

// function refreshName(newName) {
//   const str = newName.split('');
//   const balance = document.getElementById('chart-header');
//   balance.innerHTML = '';
//   (function animate() {
//   str.length > 0 ? balance.innerHTML += str.shift() : clearTimeout(running);
//   var running = setTimeout(animate, 90);
//   })();
// }

// refreshName('<%= j @group.name');
